//  arithmetic operators = operands (values, variables, etc.)
//                         operators (= - * /)

let students = 20;

// students = students + 2;
// students = students - 2;
// students = students / 2;
// students = students * 2;
// students = students ** 2;           // (power / exponential operator)
// students = students % 2;            // (modulo)

//-----OR------

// students += 2;
// students -= 2;
// students /= 2;
// students *= 2;
// students **= 2;           // (power / exponential operator)
// students %= 3;            // (modulo)

// students++;
students--;

console.log(students);

